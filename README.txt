ENGLISH

# All these scripts are for public use, but if they are published elsewhere.
# please mention this GitHub page and do not appropriate them by modifying 2 lines of code.

# To use the scripts named csv_to_fortigate_xxx you need to download the .py and the .csv files in the same directory, the .txt is optional,
# later modifiy the .csv acoording of your needs and inside the directory (cd into cmd on Windows) execute the script (.py),
# after this you get the output file, this contain the config neccesary to create the objects in the device,
# please check it out the config before copy and paste in the device, I'm not responsible if there is a problem with the config.
# note that each new execution of the .py overwrites the txt (output) file.

# I will update this Readme file if new instructions are needed.



ESPAÑOL

# Todos estos scripts son para uso público, pero si están publicados en otro lugar
# favor mencione esta página de GitHub y no se apropie de ellos modificando 2 líneas de código

# Para usar los scripts llamados csv_to_fortigate_xxx necesita descargar los archivos .py y .csv en el mismo directorio, el .txt es opcional,
# luego modifique el .csv de acuerdo a sus necesidades y dentro del directorio (cd en cmd en Windows) ejecute el script (.py),
# después de esto obtienes el archivo de salida, este contiene la configuración necesaria para crear los objetos en el device,
# por favor revise la configuración antes de copiar y pegar en el equipo , no soy responsable si hay un problema con la configuración.
# tenga en cuenta que cada nueva ejecución del .py sobreescribe el archivo txt (salida).

# Actualizaré este archivo Léame si se necesitan nuevas instrucciones.
